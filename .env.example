# copy to .env (gitignored) next to docker-compose.yml
# Prowlarr API key: Settings -> General -> Security -> API Key
PROWLARR_TOKEN=
# bearer token clients must send to the MCP endpoint; e.g. openssl rand -hex 32
PROWLARR_AUTH_TOKEN=
